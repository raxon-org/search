{{Package.Raxon.Search:Embedding:paragraph(flags(), options())}}
/**
- paragraph needs to have word count, strlen (bytes) & token count

- gzip compression and then chunks and unpack per id range,

*/